[user]
    name = Michał 'Griwes' Dominiak
    email = griwes@griwes.info

[alias]
    setworkenvironment = !git config --global user.name 'Michał Dominiak' && git config --global user.email 'michal.dominiak@nokia.com'
    ri = rebase -i --autosquash
    mri = rebase -i
    fix = commit --fixup
    squ = commit --squash

[push]
    default = simple
